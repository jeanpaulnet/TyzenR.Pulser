{
  "entities": {
    "UserState": {
      "title": "UserState",
      "description": "User specific watchlists, notes, and analyses",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "The email address of the user"
        },
        "state": {
          "type": "object",
          "description": "The complete watchlists state including symbols and analyses"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the state was last synchronized"
        }
      },
      "required": ["email", "state"]
    },
    "AnalysisCache": {
      "title": "AnalysisCache",
      "description": "Cached analysis of a market symbol to share across users",
      "type": "object",
      "properties": {
        "analysis": {
          "type": "object",
          "description": "The calculated PulserAnalysis structure"
        },
        "lastUpdated": {
          "type": "string",
          "format": "date-time",
          "description": "Server Timestamp of when the cache was written"
        }
      },
      "required": ["analysis", "lastUpdated"]
    }
  },
  "firestore": {
    "/user_states/{email}": {
      "schema": "UserState",
      "description": "Durable user profiles holding asset lists and general notebook contents"
    },
    "/analysis_cache/{symbol}": {
      "schema": "AnalysisCache",
      "description": "Shared cache containing pre-scanned analyses of active tickers"
    }
  }
}
